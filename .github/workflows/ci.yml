---
name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  happy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --locked
      - run: ./happy.sh
